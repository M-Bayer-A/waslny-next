import SigninPage from "./page";

export default function SigninLayout({ children }) {
  return (
    <div>
      <p>hello the SigninLayout</p>
      {/* <SigninPage /> */}
      {children}
    </div>
  );
}
